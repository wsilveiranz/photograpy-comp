// Placeholder shell component — no data fetching wired up yet.

export function CompetitionList() {
  return (
    <section aria-label="Competitions">
      <h2>Competitions</h2>
      <p>No competitions yet. This is an app shell — implementation to come.</p>
    </section>
  );
}
